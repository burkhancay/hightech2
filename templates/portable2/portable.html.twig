{% extends 'base.html.twig' %}

{% block title %}Tous les PC portables Windows{% endblock %}

{% block titre %}Tous les PC portables Windows{% endblock %}

{% block body %}
    <div class="row">
        {% for pc_portable in portable %}
            <div class="col-12 col-md-6 col-lg-4 col-xl-3">
                <div class="card border-light mb-3">
                    <div class="card-header text-center">
                        <img src="{{asset('img/' ~ pc_portable.image)}}" width="250" height="250">
                    </div>
                    <div class="card-body">
                        <h4 class="card-title">
                        <a href="{{ path('afficher_portable', {'id': pc_portable.id}) }}" >{{ pc_portable.marque }}</a>
                        </h4>
                        <p>prix : {{ pc_portable.prix }} €</p>
                        <a class="btn btn-success" href=""><i class="fas fa-shopping-basket"></i> Ajouter au panier</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
