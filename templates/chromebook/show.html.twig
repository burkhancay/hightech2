{% extends 'base.html.twig' %}
{% block title %}Chromebook{% endblock %}
{% block body %}
<div class="container my-5">
    <h1>Chromebook</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chromebook.id }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ chromebook.marque }}</td>
            </tr>
            <tr>
                <th>Diagonale</th>
                <td>{{ chromebook.diagonale }}</td>
            </tr>
            <tr>
                <th>Chipset</th>
                <td>{{ chromebook.chipset }}</td>
            </tr>
            <tr>
                <th>Processeur</th>
                <td>{{ chromebook.processeur }}</td>
            </tr>
            <tr>
                <th>Ram</th>
                <td>{{ chromebook.ram }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ chromebook.prix }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ chromebook.image }}</td>
            </tr>
            <tr>
                <th>Stockage</th>
                <td>{{ chromebook.stockage }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ chromebook.description }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('chromebook_index') }}">retour à la liste</a>
    <a href="{{ path('chromebook_edit', {'id': chromebook.id}) }}">modifier</a>
    {{ include('chromebook/_delete_form.html.twig') }}
    </div>
{% endblock %}