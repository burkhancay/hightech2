{% extends 'base.html.twig' %}
{% block title %}Portable{% endblock %}
{% block body %}
<div class="container my-5">
    <h1>Portable</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ portable.id }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ portable.marque }}</td>
            </tr>
            <tr>
                <th>Diagonale</th>
                <td>{{ portable.diagonale }}</td>
            </tr>
            <tr>
                <th>Chipset</th>
                <td>{{ portable.chipset }}</td>
            </tr>
            <tr>
                <th>Processeur</th>
                <td>{{ portable.processeur }}</td>
            </tr>
            <tr>
                <th>Ram</th>
                <td>{{ portable.ram }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ portable.prix }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ portable.image }}</td>
            </tr>
            <tr>
                <th>Stockage</th>
                <td>{{ portable.stockage }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ portable.description }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('portable_index') }}">retour à la liste</a>
    <a href="{{ path('portable_edit', {'id': portable.id}) }}">modifier</a>
    {{ include('portable/_delete_form.html.twig') }}
    </div>
{% endblock %}