<form method="post" action="{{ path('gamer_delete', {'id': gamer.id}) }}" onsubmit="return confirm('Etes vous sûr(e) de vouloir supprimer cet article ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gamer.id) }}">
    <button class="btn">effacer</button>
</form>